version: "3.3"

services:
  app1:
    build: .
    image: app1
    restart: always
    ports:
      - 3000:80
    external_links: 
      - proxy
    networks:
      - app-network

networks:
  app-network:
    external: 
      name: app-network
